const CACHE='meditasiku-release-v17-clean-modular';
const SHELL=['./','./index.html','./meditasiku-extra-300.js','./meditasiku-dose-data.js','./meditasiku-production.js','./meditasiku-production.css','./meditasiku-app.js','./meditasiku-app.css','./manifest.webmanifest','./icon-192.png','./icon-512.png','./apple-touch-icon.png','./assets/app/sanctuary-gateway.webp','./assets/app/splash-background.webp','./assets/app/home-background.webp','./assets/app/explore-background.webp','./assets/app/library-background.webp','./assets/app/profile-background.webp','./assets/brand/meditasiku-logo-mark.png'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==location.origin)return;event.respondWith((async()=>{const cache=await caches.open(CACHE);if(event.request.mode==='navigate'){try{const fresh=await fetch(event.request);if(fresh?.ok)cache.put('./index.html',fresh.clone());return fresh}catch{return(await cache.match('./index.html'))||(await cache.match('./'))||Response.error()}}const cached=await cache.match(event.request);if(cached)return cached;try{const fresh=await fetch(event.request);if(fresh?.ok)cache.put(event.request,fresh.clone());return fresh}catch{return Response.error()}})())});
self.addEventListener('message',event=>{if(event.data?.type==='SKIP_WAITING')self.skipWaiting();if(event.data?.type==='CACHE_URLS'&&Array.isArray(event.data.urls))event.waitUntil(caches.open(CACHE).then(async cache=>{for(const url of event.data.urls){try{await cache.add(url)}catch{}}})) });
